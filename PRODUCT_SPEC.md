# AI 독서토론 서비스 — Product Spec v1.0

> **Status:** Product baseline / Source of Truth  
> **Audience:** Product owner, Codex, implementation agents  
> **Scope:** 제품 원칙, 사용자 경험, AI 행동 정책, MVP 범위, 검증 기준  
> **Out of scope:** 기술 아키텍처, 프레임워크/스택, DB 모델, API 설계, 실시간 통신 방식, LLM 호출 구조, 배포 구조

---

## 0. 문서 사용 원칙

이 문서는 제품 요구사항의 기준선이다.

- 구현 편의를 위해 제품 원칙을 임의로 변경하지 않는다.
- 기술 설계는 이 문서에서 파생되어야 하며, 이 문서를 기술 설계에 맞추어 역으로 변경하지 않는다.
- 구현 과정에서 제품 행동을 바꿔야 할 필요가 생기면 먼저 Decision으로 기록하고 제품 결정인지 기술 결정인지 구분한다.
- 아직 검증되지 않은 수치(weight, threshold, 시간 등)는 **초기 가설**로 취급하며 실제 사용자 테스트로 보정한다.
- 이 문서의 AI 모듈 명칭(Evaluator, Policy Engine, AI Host, Living Wiki 등)은 **제품 수준의 역할/행동 계약**이다. 실제 코드 구조나 서비스 분리 방식은 기술 기획에서 결정한다.
- 프라이버시와 권한 규칙은 성능 튜닝을 위해 완화할 수 없는 절대 규칙이다.
- Living Wiki·Raw Data retrieval의 상세 구현 계약은 `docs/AI_ENGINE_SPEC.md`, Book Context Pack·Provider·Builder의 상세 구현 계약은 `docs/BOOK_CONTEXT_SPEC.md`에서 관리하되 이 문서의 제품 의미를 축소할 수 없다.

---

# 1. 제품 정의

도서관 회원 독서모임에 AI 호스트를 넣어 기존 독서토론의 진행 품질을 높이는 서비스다.

특정 문제 하나를 제거하는 도구라기보다 이미 존재하는 독서토론이 더 깊고, 더 다양한 관점을 만나고, 토론 후 생각이 남도록 돕는 것을 목표로 한다.

## 핵심 제품 원칙

> **AI의 목적은 좋은 말을 하는 것이 아니라 참가자에게서 좋은 말을 끌어내는 것이다.**

AI는 독서 선생님이나 해설자가 아니라 **독서토론 호스트**다.

---

# 2. 좋은 독서토론의 작업 정의

MVP에서 좋은 독서토론은 다음 세 가지를 핵심 가치로 본다.

## 2.1 책과의 연결

대화가 책의 문장, 장면, 인물, 주장 또는 책에서 출발한 핵심 질문과 연결된다.

단, 좋은 토론이 책 밖의 개인 경험, 사회, 가치 문제로 확장되는 것을 방해하지 않는다.

## 2.2 관점 확장

서로 다른 해석을 발견하고, 비교하고, 연결하며 기존 생각을 확장하거나 수정한다.

## 2.3 정리와 잔상

토론 이후에도 핵심 쟁점, 관점 차이, 새롭게 생긴 생각, 남은 질문이 참가자에게 남는다.

### 한 문장 정의

> 책을 중심으로 서로 다른 관점을 발견하고, 대화를 통해 생각을 확장한 뒤, 그 변화가 참가자에게 남는 토론.

---

# 3. MVP 기본 가정과 범위

- 참가자는 해당 책을 **완독한 상태**에서 참여한다고 가정한다.
- 첫 MVP는 **실시간 동기식 텍스트 토론**으로 시작한다.
- 메시지는 누가 말했는지 식별 가능해야 한다.
- 음성 인식, speaker diarization, 실시간 음성 개입은 MVP에서 제외한다.
- 기본 토론 타이머는 **30분**이며 Opening, Core Discussion, Synthesis를 포함한다. 이후 Closing을 최대 5분 별도로 진행한다.
- 방장은 종료 전 안내를 받고 토론을 15분 단위로 반복 연장할 수 있으며, AI는 필요하면 연장 또는 마무리 의견을 제공할 수 있다.
- 초기 목표는 AI 호스트 로직 자체를 검증하는 것이다.

## 3.1 계정과 토론방 접근

- 방장과 일반 참가자는 모두 계정 로그인 후 서비스를 사용한다.
- 게스트 참가는 MVP에서 제공하지 않는다.
- 사용자는 계정에 연결된 프로필을 가지며, 토론에서는 세션별 닉네임이 아니라 프로필에 설정한 이름을 사용한다.
- 계정을 통해 자신이 생성했거나 참여한 여러 토론과 해당 결과를 다시 확인하고 관리할 수 있어야 한다.
- MVP에서는 도서관 회원 여부를 별도로 확인하거나 인증하지 않는다.
- MVP의 회원가입과 로그인은 이메일과 비밀번호를 사용한다.
- 이메일은 계정 식별자로 사용하며 중복 가입을 허용하지 않는다.
- 회원가입 시 토론에서 사용할 프로필 이름을 반드시 입력한다.
- 가입 시 이메일 소유 확인 절차는 요구하지 않는다. 이메일 인증 없이 계정을 만들고 토론을 생성·참가할 수 있다.
- 비밀번호를 잊은 사용자는 입력한 이메일을 통해 재설정할 수 있어야 한다.
- 소셜 로그인은 MVP에서 제공하지 않는다.
- staging과 production의 회원가입·로그인·비밀번호 재설정 요청에는 Cloudflare Managed Turnstile 보안 확인을 공통 적용한다. 정상 요청은 가능한 한 자동 통과시키고 공급자가 추가 확인을 요구할 때만 form 안에서 상호작용을 요청한다. deterministic local 개발 환경에서는 비활성화할 수 있다.
- 토론방은 로그인한 사용자가 검색할 수 있다.
- 토론방 참가에는 해당 방의 참가 패스워드가 필요하다. 계정 로그인 비밀번호와 토론방 참가 패스워드는 서로 다른 개념이다.
- 토론방 검색 조건·결과 표시와 참가 패스워드의 생성·변경·재진입 정책은 아래 §3.3~§3.6의 확정 규칙을 따른다. 인증 공급자와 패스워드의 hash·rate limit 같은 구현 방식은 `docs/TECHNICAL_PLAN.md`와 `docs/DECISIONS.md`의 승인된 기술 기준을 따른다.

## 3.2 프로필 표시 정책

- 프로필 이름은 다른 사용자와 중복될 수 있다. 사용자의 실제 식별은 프로필 이름이 아니라 계정 ID를 기준으로 한다.
- MVP에서는 프로필 이미지 업로드를 제공하지 않고 프로필 이름의 첫 글자를 기본 아바타로 사용한다.
- 사용자는 프로필 이름을 변경할 수 있다.
- 이름을 변경해도 과거에 작성한 메시지와 마지막 한 줄에 표시된 당시 이름은 소급 변경하지 않는다.
- 변경된 이름은 변경 이후 새로 작성하는 메시지와 새 토론 참여에 적용한다.

## 3.3 토론방 생성

- 로그인한 모든 회원은 토론방을 생성할 수 있다.
- 토론방을 생성한 회원이 해당 방의 방장이 된다.
- 방장은 운영자가 Publish한 Book Context Pack이 있는 책 중 한 권을 선택해야 한다.
- Publish된 Book Context Pack이 없는 책으로는 토론방을 생성할 수 없다.
- 책 선택 화면에서는 책 제목과 저자로 Published Pack을 검색할 수 있다.
- 기본 정렬은 최근 추가된 순이며 `최근 추가된 순 / 제목 가나다순 / 저자 가나다순` 정렬을 제공한다.
- 검색 결과에는 표지, 제목, 저자, 출판사, 출간 연도를 표시해 동명 도서와 판본을 구분한다.
- 하나의 Published Pack을 하나의 선택 항목으로 표시하고, 선택 후 책 기본 정보와 짧은 소개를 확인할 수 있게 한다.
- 검색 결과가 없으면 `아직 토론이 준비된 책이 없습니다`라고 안내한다.
- 사용자가 직접 책을 등록하거나 Pack 생성을 요청하는 기능은 MVP에서 제공하지 않는다.
- Draft, 검수 중, Publish 취소 상태의 Pack은 일반 사용자에게 노출하지 않는다.
- 토론방 생성 시 방 제목, 책, 시작 예정 시각, 참가 패스워드, 최소 참가 인원, 최대 참가 인원을 반드시 입력한다.
- 방별 인원 설정은 `2 ≤ 최소 참가 인원 ≤ 최대 참가 인원 ≤ 15`를 만족해야 한다.
- 세션 시작 가능 여부의 최소 인원은 시작 시점에 대기실에 실제 접속 중인 참가 등록 회원 수로 계산한다.
- 대기실 접속 인원이 최소 참가 인원보다 적으면 세션을 시작할 수 없다.
- 최대 참가 인원은 현재 접속 여부와 관계없이 참가 등록 membership 수로 계산하며, 최대 인원에 도달하면 추가 참가를 허용하지 않는다.
- 방장은 최소 시작 인원과 최대 참가 인원 계산에 모두 포함한다.
- 참가 등록 후 대기실에서 접속을 종료한 일반 참가자는 정원을 계속 점유하지만 다시 접속하기 전까지 최소 시작 인원에는 포함되지 않는다.
- 방장은 생성 후 별도 방 설정에서 토론방 정보를 관리할 수 있다. 각 항목의 변경 가능 시점은 상세 기능 명세에서 정한다.

## 3.4 토론방 참가와 시작

- 검색 결과나 방 상세를 보는 것만으로는 해당 방의 참가자가 되거나 `내 토론`에 추가되지 않는다.
- 세션 시작 전에 로그인한 사용자가 올바른 방 참가 패스워드를 입력하면 별도 방장 승인 없이 `참가 등록` 상태가 되고 대기실로 들어간다.
- 방 참가 패스워드는 4~20자이며 영문 대소문자를 구분하고 숫자·기호를 허용한다.
- 패스워드가 틀리면 `패스워드가 올바르지 않습니다`라고 표시하고 입력값을 비운 뒤 다시 입력할 수 있게 한다.
- 반복 오입력에는 계정·방 단위 요청 제한을 적용하고 `잠시 후 다시 시도해주세요`라고 안내한다. 정확한 횟수와 제한 시간은 보안 설계에서 정한다.
- 올바른 패스워드를 입력해도 membership 생성 시점에 정원이 찼다면 참가시키지 않고 `방금 정원이 마감되었습니다`라고 안내한다.
- 정원 마감 상태에서는 신규 사용자의 패스워드 입력을 비활성화하며 대기자 명단을 제공하지 않는다.
- 정원이 다시 생기면 검색 결과와 방 상세의 참가 가능 상태를 갱신한다.
- 이미 참가 등록했거나 실제 참여한 회원은 정원 마감이나 방장의 패스워드 변경과 관계없이 패스워드 없이 재입장한다.
- 방장은 자신이 만든 방에 참가 패스워드를 입력하지 않는다.
- 같은 사용자의 참가 요청이 반복되어도 membership과 정원 점유는 하나만 생성한다.
- 방장이 내보낸 사용자에게는 패스워드 오류 대신 `이 토론방에 다시 참여할 수 없습니다`라고 안내한다.
- 취소되거나 공식 종료된 방에 직접 접근하면 해당 상태를 안내하고 신규 참가 입력을 제공하지 않는다.
- 참가 패스워드 찾기, 참가 요청, 대기자 명단은 MVP에서 제공하지 않으며 패스워드는 방장에게 서비스 밖에서 전달받는다.
- `참가 등록`은 해당 토론방의 지속되는 membership이며 최대 참가 인원에 포함된다.
- 참가 등록된 사용자는 `내 토론 > 예정`에서 방을 확인하고 선택적 사전 입력을 작성할 수 있다.
- 일반 참가자는 세션 시작 전에 참가를 취소할 수 있다. 취소하면 membership, `내 토론` 항목, 정원 점유가 제거된다.
- 방장은 자신이 생성한 방이므로 참가 등록·접속 여부와 관계없이 `내 토론`에 유지된다.
- 세션 시작 전에는 방장이 방 제목, 시작 예정 시각, 참가 패스워드, 최소 참가 인원, 최대 참가 인원을 수정할 수 있다.
- 책은 방장 외 다른 참가자가 한 명도 참가 등록하지 않은 경우에만 변경할 수 있다.
- 세션 시작 후에는 방 제목, 책, 시작 예정 시각, 최소 참가 인원을 변경할 수 없다.
- 세션 시작 후에도 방장은 참가 패스워드를 변경할 수 있다. 변경 전 이미 참가 등록된 회원의 membership과 접속에는 영향을 주지 않으며 이후 신규 참가자에게만 새 패스워드를 요구한다.
- 세션 진행 중 최대 참가 인원은 15명 이내에서 늘릴 수 있다.
- 최대 참가 인원을 줄일 때는 현재 참가 등록 membership 수보다 작게 설정할 수 없다.
- 방장은 시작 예정 시각 이후이고 최소 참가 인원이 충족된 경우 수동으로 세션을 시작한다.
- 세션이 공식 종료되기 전까지는 Core Discussion, Synthesis, Closing을 포함한 모든 진행 단계에서 신규 참가를 허용한다.
- 신규 참가는 방의 최대 참가 인원에 도달하지 않았고 올바른 참가 패스워드를 입력한 경우에만 가능하다.
- 세션 시작 후 실시간 토론방에 한 번 이상 접속하면 메시지 작성 여부와 관계없이 `실제 참여`로 확정한다.
- 세션 진행 중 처음 참가한 사용자는 실시간 토론방에 입장하는 시점에 참가 등록과 실제 참여가 함께 확정된다.
- 이미 참가한 사용자는 세션이 공식 종료되기 전까지 동일 계정으로 재접속할 수 있다.
- 세션이 공식 종료된 이후에는 신규 참가와 실시간 대화 재개를 허용하지 않는다. 실제 참여자는 `내 토론`에서 종료된 토론방에 읽기 전용으로 다시 들어갈 수 있다.
- 시작 전에 등록했지만 세션 진행 중 한 번도 실시간 토론방에 접속하지 않은 일반 참가자는 종료 후 `내 토론`에서 제거하고 대화·결과 열람권을 부여하지 않는다.
- 방장은 세션 시작 전에만 토론방을 취소할 수 있다.
- 토론방 취소는 데이터를 즉시 삭제하지 않고 방 상태를 `취소됨`으로 전환한다.
- 취소된 방은 전체 검색에서 즉시 제외하고 모든 참가 등록 membership과 정원 점유를 해제한다.
- 취소 시점에 등록되어 있던 회원의 `내 토론 > 종료`에는 해당 방을 `취소됨`으로 남겨 취소 사실을 확인할 수 있게 한다.
- 취소된 방에서는 사전 입력, 대화, 「오늘의 토론 기록」을 열람하거나 변경할 수 없다.
- 세션 시작 후에는 취소할 수 없으며 필요한 경우 방장의 `토론 종료`를 사용한다.

## 3.5 토론방 검색과 상태 표시

- 로그인한 사용자는 시작 전이거나 진행 중인 토론방을 검색할 수 있다.
- 기본 검색 기준은 방 제목, 책 제목, 저자다.
- 검색 결과에는 방 제목, 책 표지·제목·저자, 방장 프로필 이름, 시작 예정 시각, 진행 상태, 현재 참가 인원과 최대 참가 인원을 표시한다.
- 참가자 명단과 방 참가 패스워드는 검색 결과에 표시하지 않는다.
- 진행 중인 방도 검색할 수 있고, 공식 종료 전이며 정원이 남아 있으면 신규 참가할 수 있다.
- 공식 종료된 토론방은 전체 검색에서 제외하고 실제 참여자의 `내 토론`에서만 열람할 수 있다.
- 세션 진행 상태는 `시작 예정 / 시작 대기 / 토론 중 / 연장 중 / 마무리 중 / 종료`로 표시한다. 시작 전 취소된 방은 `내 토론`에서만 `취소됨`으로 표시한다.
- `시작 예정`은 예정 시각 전, `시작 대기`는 예정 시각이 지났지만 방장이 시작하지 않은 상태다.
- `토론 중`은 Opening 또는 Core Discussion, `연장 중`은 승인된 연장 토론, `마무리 중`은 Synthesis 또는 Closing, `종료`는 공식 종료 상태다.
- 참가 가능 여부는 진행 상태와 분리해 `참여 가능 / 정원 마감`으로 표시한다.
- Discussion Metrics나 AI 내부 판단 상태는 검색 결과와 참가자 화면에 노출하지 않는다.

## 3.6 실시간 메시지

- MVP는 텍스트 메시지만 지원한다.
- 메시지 한 개의 최대 길이는 2,000자이며 여러 줄 입력을 지원한다.
- 전송이 확정된 메시지는 참가자가 수정하거나 삭제할 수 없다.
- 각 메시지는 작성자와 작성 시각을 표시한다.
- 참가자는 다른 메시지를 참조하는 inline reply를 작성할 수 있다.
- inline reply는 원본 메시지 reference와 짧은 인용을 함께 보여준다.
- 메시지 전송 중, 전송 실패, 재시도 상태를 사용자에게 명확히 표시한다.
- 재시도로 같은 메시지가 중복 확정되지 않게 한다.
- emoji reaction, 이미지·파일·음성 첨부는 MVP에서 제외한다.

## 3.7 참가자 목록과 접속 상태

- 대기실에는 등록 참가자의 프로필 이름, 방장 여부, `접속 중 / 오프라인` 상태를 표시한다.
- 실시간 토론방 상단에는 현재 접속 인원을 표시하고, 전체 참가자 목록은 펼쳐서 확인할 수 있게 한다.
- 실시간 참가자 목록에는 프로필 이름, 방장 여부, 단순 접속 상태만 표시한다.
- 참가자가 메시지를 작성 중이면 다른 참가자에게 `프로필 이름 + 입력 중` 상태를 표시한다.
- 마지막 접속 시각, 발언량, 침묵 여부, 활동 점수와 Discussion Metrics는 참가자에게 노출하지 않는다.
- 화면 이탈이나 일시적인 연결 끊김은 참가 등록 또는 실제 참여 기록을 취소하지 않는다.
- AI는 presence, typing, 참가 시각을 내부 판단 신호로 사용할 수 있지만 이를 참가자 평가값으로 노출해서는 안 된다.

## 3.8 중도 이탈과 재접속

- 브라우저를 닫거나 다른 화면으로 이동하는 것은 일시적인 접속 종료이며 참가 등록이나 실제 참여를 취소하지 않는다.
- 토론 중 `나가기`를 선택하면 공식 종료 전까지 다시 들어올 수 있다는 확인을 보여주고 현재 화면에서만 나간다.
- 세션 시작 후 참가자는 자신의 참여 기록을 완전히 철회하거나 정원 점유를 해제할 수 없다.
- 공식 종료 전까지 동일 계정으로 언제든 재입장할 수 있다.
- 재접속 시 놓친 메시지, 현재 의제와 세션 단계, 남은 시간, 참가자 상태를 동기화한 뒤 메시지 입력을 허용한다.
- 연결 복구 중에는 상태를 명확히 표시하고 메시지 입력을 잠시 비활성화한다.
- AI는 연결이 끊긴 참가자를 침묵 중인 참가자로 평가하거나 발언 대상으로 지목하지 않는다.
- 세션 종료 시 오프라인이어도 실제 참여자는 종료 후 토론 기록과 결과 열람권을 유지한다.
- 중도 이탈 횟수와 접속 시간은 다른 참가자에게 공개하지 않는다.

---

# 4. 사전 입력

사전 입력은 **선택사항**이다. 입력하지 않아도 토론 참여에 제한이 없다.

가능한 입력:

1. 좋아했던 문장 + 그 문장에서 파생된 자신의 생각
2. 가장 인상 깊었던 장면 / 주장 / 부분
3. 다른 사람들과 이야기하고 싶은 질문

각 항목은 참가자가 공개 범위를 선택한다.

## 4.1 공개 범위

### PUBLIC

- 다른 참가자에게 보여줄 수 있다.
- AI가 토론 중 해당 참가자의 발언과 직접 연결할 수 있다.

### AI_PRIVATE

- AI의 의제 선정과 내부 판단에는 사용할 수 있다.
- 다른 참가자에게 원문, 표현, 출처, 작성자를 직접 또는 간접적으로 노출해서는 안 된다.
- AI Host가 참가자에게 말할 때 비공개 원문을 그대로 전달해서는 안 된다.
- 필요하면 “일부 참가자에게 다른 관점이 존재함”과 같은 **추상화된 내부 신호**만 사용할 수 있다.

## 4.2 운영 원칙

사전 입력은 숙제가 아니라 토론 품질을 높이기 위한 선택적 준비 데이터다.

## 4.3 작성·열람 시점

- 참가 등록된 회원은 대기실에서 사전 입력을 작성할 수 있다.
- 세션 시작 전까지 항목을 작성·수정·삭제하고 각 항목의 `PUBLIC / AI_PRIVATE` 공개 범위를 변경할 수 있다.
- 세션이 시작되면 모든 사전 입력과 공개 범위를 잠그고 이후 변경을 허용하지 않는다.
- `PUBLIC` 항목은 참가 등록된 회원들이 시작 전 대기실에서 작성자의 프로필 이름과 함께 열람할 수 있다.
- 실시간 토론 중에는 별도의 사전 입력 패널을 노출하지 않는다. AI는 `PUBLIC` 항목을 작성자의 현재 발언과 직접 연결할 수 있다.
- 종료된 토론방에서 실제 참여자는 당시 `PUBLIC` 항목을 읽기 전용으로 열람할 수 있다.
- `AI_PRIVATE` 항목은 작성자 본인과 허용된 AI 내부 처리 경로 외에는 시작 전·진행 중·종료 후 어떤 단계에서도 표시하지 않는다.
- 이미 시작된 세션에 새로 참가하는 회원은 사전 입력을 작성하지 않고 현재 토론에 바로 합류한다.

## 4.4 시작 후 신규 참가 안내

세션 시작 후 참가 패스워드를 통과한 회원에게 실시간 토론방 진입 전에 다음을 안내한다.

- 현재 진행 상태: `토론 중 / 연장 중 / 마무리 중`
- 현재 의제 또는 현재 정리 단계
- 남은 시간
- 현재 참가 인원 / 최대 참가 인원

사용자가 `토론에 참여하기`를 선택해 실시간 토론방에 들어가면 실제 참여자로 확정한다.

- 기존 참가자의 `PUBLIC` 사전 입력은 이 진입 화면에 별도로 표시하지 않는다.
- `마무리 중`인 경우 토론이 현재 마무리 단계라는 사실을 명확히 알린다.
- 늦은 참가자의 참가 시점을 보존하고 AI가 해당 사용자를 기존 침묵 참가자로 오인하지 않게 한다.

---

# 5. 의제 선정

AI가 모든 질문을 미리 정하지 않고, 참가자와 AI가 혼합해서 의제를 만든다.

참가자 입력에서 강한 신호가 있으면 이를 우선하고, 신호가 약하거나 편향되어 있으면 AI가 책 전체 맥락에서 보완한다.

## 참가자 의제를 우선할 조건

- **관심도:** 여러 참가자가 같은 장면, 주제, 질문을 언급하는가
- **관점 차이:** 같은 주제에 서로 다른 해석이나 입장이 존재하는가
- **책과의 연결성:** 책의 장면, 주장, 인물과 연결되는가
- **확장 가능성:** 단순 감상으로 끝나지 않고 토론 가능한 문제로 발전할 수 있는가

## 핵심 원칙

> 참가자는 **무엇이 중요했는지**를 제공하고, AI는 **왜 중요한지와 어디까지 확장할지**를 설계한다.

실제 토론 중 더 좋은 쟁점이 등장하면 준비된 질문보다 **실제 대화에서 생긴 쟁점**을 우선할 수 있다.

---

# 6. AI 호스트의 개입 철학

AI는 고정적인 진행자형이나 보조자형으로 행동하지 않는다.

토론 상황에 따라 개입 강도를 바꾸되 기본 원칙은 **최소 필요 개입**이다.

> 사람들끼리 좋은 토론을 하고 있다면 AI가 조용히 있는 것이 최고의 호스팅일 수 있다.

개입의 기본 흐름:

**Observe → Detect → Intervene → Withdraw**

AI는 개입했을 때 얻는 가치가 자연스러운 인간 대화를 끊는 비용보다 클 때만 개입한다.

---

# 7. Discussion Metrics

토론을 하나의 enum 상태로 단순 분류하지 않는다.

Evaluator는 독립적인 7개 지표를 `LOW / MEDIUM / HIGH`로 평가한다.

한 지표의 값만으로 행동을 자동 결정하지 않고, 지속 여부와 다른 지표 조합을 함께 본다.

## 7.1 Depth

**질문:** 대화가 단순 감상을 넘어 이유, 근거, 해석, 가정, 반론 수준으로 깊어지고 있는가?

- **LOW:** 감상/평가/단순 동의 중심. 이유와 근거가 거의 없음.
- **MEDIUM:** 이유 또는 책 속 근거가 붙음.
- **HIGH:** 해석, 가정, 관점 비교, 전제 검토, 반론, 자기 의견 수정 등이 발생.

정책:
- LOW 한 번은 관찰.
- LOW가 지속되면 `DEEPEN` 검토.
- HIGH이면 원칙적으로 개입을 줄인다.

초기 weight 가설: **0.75**

## 7.2 Expansion

**질문:** 새로운 관점이 등장하고 기존 관점과 연결·대조·통합되며 사고 범위가 넓어지는가?

- **LOW:** 같은 관점이나 찬반이 반복됨.
- **MEDIUM:** 새 관점은 나오지만 기존 논의와 연결이 약함.
- **HIGH:** 새 관점이 기존 관점과 연결/충돌/통합되며 새 쟁점으로 발전.

Novelty와 Connection을 함께 본다.

정책:
- HIGH이면 `WAIT`가 기본.
- LOW가 지속되면 AI 자신의 관점을 바로 주입하기보다 참가자 안의 다른 관점을 먼저 끌어낸다.

초기 weight 가설: **0.90**

## 7.3 Book Grounding

**질문:** 현재 대화가 책 자체 또는 책에서 출발한 핵심 질문과 연결 고리를 유지하는가?

책을 직접 언급하는 빈도만으로 평가하지 않는다.

- 책의 구체적 장면/문장/주장에 근거하면 직접 Grounding은 높다.
- 책에서 출발해 개인 경험, 사회, 가치 문제로 의미 있게 확장될 수 있다.
- `Book Grounding LOW + Relevance HIGH + Expansion HIGH`는 좋은 토론일 수 있다.

정책:
- 좋은 확장이 진행 중이면 책으로 강제로 되돌리지 않는다.
- 연결 고리가 약해지거나 확장이 둔화될 때 책을 새로운 사고 자극으로 다시 사용한다.
- 지향하는 흐름은 **책 → 확장 → 재접속**이다.

초기 weight 가설: **0.70~0.75**

## 7.4 Saturation

**질문:** 현재 논점을 더 이야기했을 때 새로운 관점, 근거, 질문이 나올 여지가 얼마나 남아 있는가?

- **LOW:** 새로운 관점/근거/질문이 계속 나옴.
- **MEDIUM:** 주요 관점은 어느 정도 나왔지만 탐색 여지가 있음.
- **HIGH:** 기존 주장과 근거가 반복되고 새 정보나 관점 변화가 거의 없음.

정책:
- HIGH라고 바로 전환하지 않는다.
- `Saturation HIGH + Expansion LOW`이면 마지막 확장 질문을 한 번 시도.
- 이후에도 새 가치가 없으면 `SUMMARIZE → TRANSITION`.

초기 weight 가설: **0.70**

## 7.5 Participation Balance

**질문:** 발언량이 아니라 여러 활성 참가자의 의미 있는 관점이 실제 토론에 반영되고 있는가?

Meaningful contribution의 예:
- 자신의 입장
- 이유/근거
- 반론
- 질문
- 새로운 관점

단순한 “맞아요”, “저도요”는 의미 있는 관점으로 보지 않을 수 있다.

정책:
- LOW 단독으로 좋은 흐름을 끊지 않는다.
- 자연스러운 소강점이나 의제 전환 시점에 참여를 초대한다.
- 개입 강도:
  1. 전체 초대
  2. 다른 관점 초대
  3. 필요하면 특정 참가자 지목
- 개인 지목은 기본적으로 최소화한다.

초기 weight 가설: **0.60**

## 7.6 Relevance

**질문:** 현재 대화가 지금 탐구 중인 핵심 논점과 얼마나 연결되어 있는가?

Book Grounding과 분리한다.

책을 직접 언급하지 않아도 현재 논점을 개인 경험이나 사회 사례로 탐색하면 Relevance는 높을 수 있다.

정책:
- 개별 잡담 한두 개에는 반응하지 않는다.
- LOW가 **지속될 때** 논점 복귀를 검토한다.
- 현재 논점에서 벗어나도 가치 있는 새 쟁점이면 Open Question 또는 새 의제 후보로 보존할 수 있다.

초기 weight 가설: **0.80**

## 7.7 Activity

**질문:** 현재 토론이 실제로 살아 움직이며 참가자 사이의 발언과 반응이 이어지는가?

활동량 자체는 토론 품질이 아니라 **대화의 생명 신호**다.

고려 가능한 신호:
- 최근 발언 여부
- 발언 간격
- 최근 메시지 수
- 고유 발언자 수
- 참가자 간 상호반응
- 가능한 경우 typing 상태

정책:
- 침묵 자체를 정체로 간주하지 않는다.
- 복잡한 질문 직후에는 생각할 시간을 허용한다.
- Activity LOW이면 `REVIVE`를 한 번 시도.
- `Activity LOW + Expansion LOW + Saturation HIGH` 등 다른 악화 신호가 함께 있을 때 전환을 검토.

초기 시간 가설:
- 30~45초 침묵: 기본 관찰
- 60~90초 무응답: LOW 후보

초기 weight 가설: **0.50**

---

# 8. 잡담과 주제 이탈

모든 책 밖 이야기를 나쁜 것으로 취급하지 않는다.

## RELEVANT

책, 인물, 주장, 현재 질문과 직접 연결됨.

→ 개입하지 않는다.

## RELATED_TANGENT

책에서 출발한 개인 경험, 사회 이슈, 다른 작품 등.

→ 의미 있는 확장이면 허용한다. 필요할 때 책으로 다시 연결한다.

## IRRELEVANT / OFF_TOPIC

현재 논점과 실질적 연결이 없는 잡담.

→ 한두 메시지는 무시한다. 대화 흐름 자체가 잡담으로 바뀔 때만 복귀 개입한다.

---

# 9. Policy Engine 제품 규칙

Policy Engine의 목적은 지표를 하나의 총점으로 만드는 것이 아니라 **현재 인간 대화를 최대한 보존하면서 가장 필요한 한 가지 행동을 선택하는 것**이다.

Evaluator가 여러 문제를 발견해도 한 판단 주기에서 AI Host는 원칙적으로 **한 가지 개입 목표**만 가진다.

## 9.1 최상위: 좋은 흐름을 보존

`Expansion HIGH + Relevance HIGH + Activity HIGH`처럼 대화가 살아 있고 새로운 관점이 연결되고 있다면 다른 지표에 일부 문제가 있어도 기본 행동은 `WAIT`.

## 9.2 Relevance 복구

- Relevance LOW 한 번만으로 개입하지 않는다.
- LOW가 지속되고 가치 있는 새 의제도 아니라면 현재 논점으로 연결형 복귀.
- 책에서 멀어진 것보다 **현재 논점과 연결이 끊어진 것**을 우선적으로 문제로 본다.

## 9.3 Activity가 낮을 때

- `Activity LOW + Saturation LOW/MEDIUM` → `REVIVE`
- `Activity LOW + Saturation HIGH + Expansion LOW` → `SUMMARIZE → TRANSITION`
- 사고가 필요한 질문 직후나 typing 신호가 있으면 `WAIT`

## 9.4 Saturation이 높을 때

- Expansion이 계속 HIGH이면 유지.
- `Saturation HIGH + Expansion LOW` → 마지막 확장 시도 1회.
- 여전히 새 가치가 없으면 `SUMMARIZE → TRANSITION`.

## 9.5 Expansion과 Depth의 충돌

기본 원칙:

> 먼저 관점의 공간을 확보하고, 그다음 중요한 관점을 깊게 판다.

- 관점이 거의 없음 + Expansion LOW + Depth LOW → `EXPAND`
- 서로 다른 관점이 이미 충분함 + Depth LOW → `DEEPEN`
- Expansion HIGH + Depth LOW → 나온 관점의 이유/근거/전제 탐색
- Depth HIGH + Expansion LOW → 한 관점을 깊게 파는 가치가 있으면 즉시 개입하지 않을 수 있음

## 9.6 Book Grounding

Book Grounding은 강제 복귀 규칙이 아니라 **재접속 자원**이다.

- `Grounding LOW + Relevance HIGH + Expansion/Depth HIGH` → `WAIT`
- Relevance는 유지되지만 Expansion이 둔화될 때 → `RECONNECT_TO_BOOK` 검토
- Grounding과 Relevance가 함께 약해질 때 → 책을 앵커로 복귀 가능
- 주요 의제 마무리 전 오랫동안 Grounding이 낮았다면 → `RECONNECT_AND_REFLECT` 검토

## 9.7 Participation Balance

- LOW 단독으로 좋은 흐름을 끊지 않는다.
- 소강점, 의제 전환 직전, 새 관점이 필요한 순간에 `INVITE`.
- 균등한 발언량이 아니라 **다양한 참가자의 생각이 반영됐는지**를 본다.

## 9.8 행동 후보

MVP 제품 행동 후보:

- `WAIT`
- `DEEPEN`
- `EXPAND`
- `RECONNECT`
- `RECONNECT_TO_BOOK`
- `RECONNECT_AND_REFLECT`
- `INVITE`
- `REVIVE`
- `SUMMARIZE`
- `TRANSITION`
- `RECOMMEND_EXTENSION`

프라이버시와 권한 규칙은 이 모든 행동보다 상위의 절대 규칙이다.

---

# 10. 30분 토론 + 최대 5분 Closing Flow

시간은 강제 스크립트가 아니라 운영 가이드다.

목표는 30분을 채우는 것이 아니라 **의미 있는 토론 하나를 완결하는 것**이다.

기본 토론 타이머 30분에는 Opening, Core Discussion, Synthesis가 포함된다. 토론 타이머가 끝난 뒤 Closing을 최대 5분 진행하므로 연장이 없는 세션은 공식 종료까지 최대 약 35분이다.

## 10.1 Opening — 약 3~5분

AI는 긴 사용 설명이나 전체 Agenda를 먼저 보여주지 않는다.

첫 의제:
- 사전 입력이 충분하면 공통 관심 + 관점 차이가 함께 나타나는 지점
- 입력이 약하면 Book Context의 discussion-worthy issue

첫 질문은 지식 확인보다 **입장을 드러내는 질문**을 우선한다.

Opening 목표:

> 첫 3~5분 안에 서로 다른 입장 또는 해석이 최소 2개 이상 드러나게 한다.

준비된 전체 Agenda는 내부 후보로 유지하고 참가자에게는 현재 의제만 보여준다.

## 10.2 Core Discussion — 약 18~22분

30분 세션 기준 주요 의제 약 2개를 준비할 수 있지만 반드시 모두 소진할 필요는 없다.

내부 의제 개념:
- Primary Agenda 1개
- Backup Agenda 1~2개
- 대화 중 생성되는 Emergent Agenda

운영 원칙:
- Depth/Expansion/Activity가 좋으면 한 의제를 오래 유지해도 된다.
- 빠르게 Saturation이 높아지면 전환한다.
- 실제 대화에서 더 좋은 쟁점이 나오면 준비된 Agenda보다 Emergent Agenda를 우선할 수 있다.

한 의제의 이상적 흐름:

**입장 노출 → 관점 차이 발견 → 필요 시 EXPAND/DEEPEN → 의미 있는 책 밖 확장 허용 → 필요 시 책 재접속 → 포화 시 마지막 확장 → 정리/전환**

## 10.3 종료 임박 시 연장 판단

Synthesis보다 먼저 연장 여부를 판단한다.

기본 흐름:

**Continue? → 방장 결정 → 15분 연장 또는 Synthesis → Closing**

기본 토론 또는 연장 구간의 남은 시간이 7분이 되면 모든 참가자에게 종료 임박 상태와 AI 의견을 보여주고, 방장에게 `15분 연장 / 마무리하기`를 묻는다.

AI는 다음 신호를 바탕으로 `연장 추천` 또는 `마무리 추천` 의견을 함께 제공할 수 있다.

- Activity가 살아 있음
- Expansion 또는 Depth가 높음
- Saturation이 HIGH가 아님
- 종료 직전 가치 높은 Emergent Agenda가 생김

- 방장은 AI 의견과 관계없이 연장을 선택할 수 있으며 최종 결정권을 가진다.
- 기본 연장 단위는 15분이고 연장 횟수에는 제한을 두지 않는다.
- 자동 연장은 없으며 매 연장 구간이 끝나기 전에 방장의 명시적인 재승인을 받는다.
- AI 의견 생성이 실패해도 방장은 정상적으로 연장 또는 마무리를 선택할 수 있다.
- 방장에게는 남은 시간이 7분에서 5분이 될 때까지 2분간 결정 시간을 제공한다.
- 방장이 `마무리하기`를 선택하면 즉시 Synthesis로 전환한다.
- 방장이 남은 시간 5분까지 응답하지 않으면 자동으로 Synthesis에 들어간다.
- 방장이 연장을 선택하면 그 시점에 토론 타이머를 15분 늘리고 `연장 중`으로 전환한다.
- 방장이 오프라인이고 권한 이전도 이루어지지 않았다면 응답 없음으로 처리한다.
- 연장 결정과 변경된 종료 시각은 모든 참가자에게 알린다.
- 연장 중 신규 참가와 기존 참가자의 재접속을 허용하며, 다른 참가자는 자유롭게 화면을 나갈 수 있다.
- 일반 참가자 투표와 자동 다수결은 제공하지 않는다.
- 방장은 연장 중에도 세션을 강제 종료할 수 있다.
- 연장된 토론 타이머가 다시 7분 남으면 동일한 결정 절차를 반복한다.

## 10.4 Synthesis — 마지막 약 5~7분

Synthesis는 단순 요약이 아니라 **관점 구조와 생각의 변화·잔상을 굳히는 단계**다.

진행:

1. 주요 관점 2~4개와 갈린 지점을 정리
2. 불일치를 억지로 하나의 결론으로 합치지 않음
3. 짧은 reflection 요청  
   - 예: “오늘 이야기하면서 처음 생각과 달라졌거나 새롭게 보이게 된 부분이 있나요?”
4. 책 밖으로 많이 확장됐다면 책으로 마지막 재접속
   - 예: “지금까지 나눈 이야기를 가지고 다시 이 장면을 보면 처음과 달라 보이는 부분이 있나요?”

AI는 작품의 정답이나 최종 해석을 선언하지 않는다.

## 10.5 Closing

Synthesis가 끝나면 새로운 의제를 더 열지 않는다.

토론 타이머가 0이 되면 Closing을 시작한다. Closing은 최대 5분 동안 진행하며 일반 채팅 입력을 닫고 기존 대화는 계속 열람할 수 있게 한다.

실제 참여자는 선택적으로 마지막 한 줄을 남기거나 `건너뛰기`를 선택할 수 있다.

예:
> 오늘 대화 후 가장 남는 생각을 한 문장으로 남겨주세요.

이미 Synthesis에서 충분한 reflection을 받았다면 중복 입력을 강요하지 않는다.

- 마지막 한 줄은 참가자별 하나, 최대 300자다.
- Closing이 끝나기 전까지 자신의 마지막 한 줄을 수정하거나 삭제할 수 있다.
- 다른 참가자의 마지막 한 줄은 서로의 작성에 영향을 주지 않도록 Closing 도중 공개하지 않는다.
- 현재 접속 중인 실제 참여자 전원이 제출 또는 건너뛰기를 선택하면 5분 전에도 공식 종료할 수 있다.
- 응답하지 않거나 오프라인인 참가자가 있어도 5분이 지나면 자동으로 공식 종료한다.
- 방장이 일반 종료 흐름에서 Closing만 임의로 조기 마감하는 기능은 제공하지 않는다. 긴급한 경우 `토론 종료`를 사용한다.
- Closing 중 재접속하거나 새로 참가한 사람도 남은 시간 안에 제출할 수 있지만 마감 시각은 연장하지 않는다.
- 서버가 마감 전에 확정한 입력만 결과에 포함한다.
- 공식 종료 후 마지막 한 줄은 수정하거나 삭제할 수 없다.
- 결과에는 제출 당시 프로필 이름을 표시하며 이후 프로필 이름 변경으로 과거 기록을 바꾸지 않는다.
- 제출하지 않은 참가자는 결과에 별도로 표시하지 않는다.
- Closing에서는 AI Evaluator와 자동 개입을 중지하고 새로운 AI 질문이나 주제를 만들지 않는다.

세션이 공식 종료되면 토론 결과를 확정한다.

공식 결과는 종료 이후 임의로 계속 바뀌지 않는다.

개인 메모 기능이 생기더라도 공식 토론 기록과는 분리한다.

## 10.6 방장의 토론 종료

- 세션 시작 후에는 방장만 언제든 `토론 종료`를 실행할 수 있다. 시작 전에는 `토론방 취소`를 사용한다.
- 실행 전에 모든 참가자의 메시지 입력이 끝나고 같은 세션을 다시 시작할 수 없다는 확인을 보여준다.
- 종료 사유 입력과 참가자 투표는 MVP에서 제공하지 않는다.
- 확정 즉시 신규 참가, 재접속, 메시지 전송, 연장 결정을 모두 막는다.
- 종료 전에 서버에서 확정된 메시지는 보존하고, 종료와 동시에 처리 중이던 미확정 메시지는 전송 실패로 처리한다.
- 모든 참가자에게 `방장이 토론을 종료했습니다`를 알리고 세션 상태를 `종료`로 전환한다.
- 종료 동작은 취소할 수 없고 같은 세션을 다시 열 수 없다.
- Core Discussion이나 연장 중 종료하면 실시간 Synthesis와 Closing은 생략한다.
- Synthesis나 Closing 중 종료하면 그 시점까지 서버에서 확정된 입력만 사용한다.
- AI는 종료 시점까지의 Raw Data로 「오늘의 토론 기록」을 비동기로 생성한다.
- 기록을 만들기에 대화가 충분하지 않다면 내용을 꾸며내지 않고 `기록을 만들기에 충분한 대화가 없습니다`라고 표시한다.
- 종료 전에 확정된 마지막 한 줄은 결과에 포함하며 작성하지 못한 참가자에게 추가 입력을 요구하지 않는다.
- 실제 참여자는 일반 종료와 동일하게 `내 토론 > 종료`에서 대화와 결과를 열람한다.
- 결과 생성이 실패해도 세션 종료는 유지하며 결과 화면에는 생성·재시도 상태를 표시한다.

---

# 11. 「오늘의 토론 기록」

최종 결과물은 회의록이나 발언 시간순 요약이 아니다.

> 오늘 어떤 생각들이 나왔고, 어떻게 갈렸으며, 어디까지 확장되었는가를 남기는 공동 사고 기록.

준비된 Agenda가 아니라 **실제로 의미 있게 다뤄진 내용**을 기준으로 생성한다.

## 11.1 오늘의 핵심 쟁점

실제로 중요하게 다뤄진 쟁점 약 2~4개.

## 11.2 관점 지도

각 쟁점에서 나온:
- 주요 관점
- 서로의 충돌
- 연결
- 제3의 시각

AI는 서로 다른 관점을 억지로 하나의 결론으로 합치지 않는다.

## 11.3 오늘 생긴 변화와 확장

기록 대상:
- 기존 입장의 보완/수정
- 새롭게 등장한 관점
- 새 질문
- 책에서 개인 경험/사회/가치 문제로의 확장

주의:
- 참가자의 생각이 실제로 바뀌었다고 단정할 근거가 부족하면 “변화했다”고 쓰지 않는다.
- “새로운 관점이 제시되었다”처럼 사실에 가까운 표현을 사용한다.

## 11.4 남은 질문

- 결론 나지 않은 질문
- 새롭게 열린 질문
- 다음에도 생각해볼 문제

## 11.5 이름 노출 정책

기본 결과물의 쟁점·관점·변화·남은 질문은 **참가자 이름을 붙이지 않고 익명화**한다.

참가자가 Closing에서 **직접 작성한 마지막 한 줄**은 이름과 함께 표시할 수 있다.

원본 채팅과 내부 근거에는 발언자 식별 정보를 보존한다.

MVP에서는 참가자별 AI 분석 리포트를 별도로 만들지 않는다.

## 11.6 종료된 토론의 열람과 공유

- 방장과 세션에 실제 참여한 계정만 종료된 토론방에 다시 들어갈 수 있다.
- 참가 등록만 하고 실시간 토론방에 들어오지 않은 일반 참가자는 열람할 수 없다.
- 방장이 내보낸 참가자는 자신의 과거 메시지가 보존되어도 종료된 방과 결과를 열람할 수 없다.
- 종료된 방에서는 방·책 정보, 실제 참여자 목록, 전체 채팅과 AI 발언, PUBLIC 사전 입력, 「오늘의 토론 기록」, 제출된 마지막 한 줄을 읽기 전용으로 제공한다.
- AI_PRIVATE 사전 입력은 작성자 본인만 다시 볼 수 있고 다른 참가자에게는 계속 공개하지 않는다.
- 내부 AI 평가, Discussion Metrics, 운영 로그는 방장과 참가자에게 공개하지 않는다.
- 종료 후 메시지, 사전 입력, 마지막 한 줄, 공식 토론 기록은 참가자와 방장이 수정하거나 삭제할 수 없다.
- 방장도 종료된 방에 새로운 사람을 추가하거나 열람 권한을 부여할 수 없다.
- MVP에서는 공개 공유 링크, 비회원 공개, SNS 공유, 파일 다운로드·내보내기를 제공하지 않는다.
- 종료된 방은 전체 검색에서 제외하고 `내 토론 > 종료`에서만 접근한다.
- 결과 생성 중에도 채팅을 먼저 열람할 수 있으며 결과 영역에는 `생성 중 / 재시도 중` 상태를 표시한다.
- 별도의 자동 만료 기간을 두지 않고 이후 확정할 데이터 보관·탈퇴·삭제 정책에 따라 유지한다.

---

# 12. AI, 방장, 일반 참가자 권한

## AI 호스트

토론 진행 담당:
- 질문
- 관점 연결
- 책으로 재접속
- 주제 전환
- 중간 정리
- 연장 추천

세션 운영의 최종 권한은 갖지 않는다.

AI 지연·실패 규칙:

- 자동 평가와 자동 개입에는 참가자-facing `AI가 생각 중` 상태를 표시하지 않으며, 실패해도 인간 대화를 계속할 수 있어야 한다.
- 방장의 `AI 도움 요청`을 처리하는 동안에는 방장에게만 `AI가 흐름을 확인 중` 상태를 표시한다.
- 방장 요청이 실패하면 방장에게만 재시도 안내를 보여주고 다른 참가자에게 기술 오류를 노출하지 않는다.
- Opening과 Synthesis처럼 AI 발언이 예정된 단계에서 응답이 지연되면 모든 참가자에게 간단한 `AI 응답 준비 중` 상태를 표시하되 채팅은 계속 허용한다.
- Opening 생성이 최종 실패하면 미리 준비된 기본 시작 질문으로 토론을 시작한다.
- Synthesis 생성이 최종 실패하면 실패 안내 후 세션을 멈추지 않고 Closing으로 넘어간다.
- 연장에 대한 AI 의견 생성이 최종 실패해도 방장의 연장·마무리 선택은 그대로 제공한다.
- 최종 토론 기록의 생성 지연이나 실패는 세션의 공식 종료를 막지 않는다. 결과 화면에는 `생성 중` 또는 `재시도 중` 상태를 표시한다.
- 원본 오류 메시지, 모델명, 내부 점수는 사용자에게 노출하지 않는다.
- 자동 재시도 횟수와 지연 표시 기준은 기술 설계에서 정한다.
- 재시도로 AI 메시지나 결과물이 중복 생성되어서는 안 된다.

## 방장

MVP 운영 권한:
- 세션 시작
- 종료 전 15분 단위 반복 연장 또는 마무리 결정
- 필요 시 강제 종료
- 심각한 상황에서 AI 개입 요청
- 참가자 내보내기와 해당 방 재참가 차단
- 다른 참가자에게 방장 권한 이전

원칙:

> 방장은 세션 운영 권한만 가지고 토론 진행 로직은 기본적으로 AI에게 맡긴다.

AI 도움 요청 규칙:

- 방장에게만 `AI 도움 요청` 기능을 제공한다.
- Opening, Core Discussion, 연장 토론 중에만 요청할 수 있고 Synthesis와 Closing에서는 사용할 수 없다.
- 요청 사유는 `대화가 멈춤 / 논의가 반복되거나 막힘 / 주제에서 너무 벗어남 / 갈등으로 흐름 정리가 필요함` 중 하나를 선택하며 자유 입력은 제공하지 않는다.
- 요청이 접수되면 AI는 최근 대화와 선택된 사유를 확인하고 한 번의 사용자-facing 개입 메시지를 반드시 작성한다.
- 방장은 구체적인 질문, 결론, AI 행동을 지정할 수 없으며 개입 목표와 내용은 AI가 결정한다.
- 요청으로 생성된 AI 메시지에는 `방장 요청으로 개입`임을 표시해 자동 개입과 구분한다.
- 하나의 요청을 처리하는 동안 중복 요청을 허용하지 않는다. 추가 요청 제한 시간은 기술 설계 또는 실험값으로 정한다.
- AI 요청이나 생성이 실패해도 인간 대화와 세션 진행은 계속되어야 한다.
- 참가자에 대한 심각한 운영 문제는 AI 도움 요청이 아니라 방장의 참가자 내보내기 권한으로 처리한다.

참가자 관리 규칙:

- 방장은 시작 전 대기실이나 진행 중인 토론에서 참가자를 내보낼 수 있다.
- 내보낸 참가자는 해당 방에 다시 참가할 수 없다.
- 내보내기 전 대상 참가자 이름과 영향을 보여주는 확인 절차를 거친다.
- 내보낸 참가자의 기존 메시지와 Raw Data는 삭제하지 않는다.
- 내보낸 참가자는 해당 방, 종료된 대화, 「오늘의 토론 기록」을 더 이상 열람할 수 없다.
- 내보내기로 해제된 membership은 최대 참가 인원에서 제외되어 자리가 다시 열린다.
- 시작 전에는 참가 등록된 회원에게, 시작 후에는 실제 참여자에게 방장 권한을 이전할 수 있다.
- 권한 이전이 완료되면 기존 방장은 일반 참가자가 된다.
- MVP에서는 공동 방장을 지원하지 않는다.
- 일반 참가자의 신고·차단 기능은 MVP에서 제외한다.

방장에게 노출하지 않는 정보:
- 참가자별 Depth 등 내부 평가 점수
- AI_PRIVATE 사전 입력 원문
- Participant State 내부 추론
- Evaluator/Policy 내부 판단 로그

## 일반 참가자

- 사전 입력 작성
- PUBLIC / AI_PRIVATE 선택
- 실시간 채팅 참여
- 현재 의제 확인
- Closing 마지막 한 줄 작성
- 종료 후 「오늘의 토론 기록」 열람
- 연장 세션 계속 참여 또는 자유 퇴장

---

# 13. AI 내부 역할의 제품 계약

아래는 기술 아키텍처가 아니라 **역할 정의**다.

## 13.1 Evaluator

질문:
> 지금 토론이 어떤 상황인가?

참가자에게 직접 말하지 않는다.

주요 결과:
- 7개 Discussion Metrics
- 현재 실제 논점
- 주요 관점
- 토론에서 실제 언급된 책 근거
- 참여 상태
- 개입 필요성 보조 신호
- 판단 근거
- 판단 confidence(실제 확률로 간주하지 않음)

## 13.2 Policy Engine

질문:
> 그래서 지금 무엇을 해야 하는가?

Evaluator의 판단과 제품 정책을 바탕으로 한 가지 행동 목표를 선택한다.

## 13.3 AI Host

질문:
> 선택된 행동을 참가자에게 어떻게 자연스럽게 말할 것인가?

필요한 경우에만 실제 참가자용 문장을 생성한다.

---

# 14. 토론 평가 Trigger의 제품 원칙

토론 상태 평가는 메시지 하나마다 고정적으로 실행하는 것을 요구하지 않는다.

다음 신호를 함께 고려한다.

- 메시지 누적
- 고유 발언자 수
- 실제 활성 참가자 수
- 침묵
- 종료 임박
- 한 의제의 장기화
- 연장 판단 시점

초기 실험 가설:
- 활성 참가자의 약 50~70%가 발언했거나 충분한 대화 묶음이 형성되면 평가 후보

정확한 실행 방식과 최적 threshold는 기술 기획/실험에서 결정한다.

---

# 15. Session Memory — Living Wiki

AI는 최근 몇 개 메시지만 보지 않고 세션 전체의 사고 구조를 압축해 유지하는 **Living Wiki** 개념을 사용한다.

## 15.1 핵심 원칙

- **Raw Data:** 수정할 수 없는 사실의 원본
- **Living Wiki:** AI가 갱신할 수 있는 세션 내 구조화된 해석
- Living Wiki는 참가자에게 보여주는 공동 문서가 아니라 Evaluator와 Host가 세션 전체의 사고 구조를 놓치지 않기 위해 사용하는 내부 작업 기억이다.
- Living Wiki의 문장과 구조는 사실 원본이 아니라 언제든 원본으로 재검증되어야 하는 해석이다.
- Living Wiki가 불충분하거나 모호하면 필요한 원본을 다시 확인할 수 있어야 한다.
- Living Wiki는 **한 토론 세션 안에서만** 사용한다.
- 세션 간 장기 기억은 MVP에서 제외한다.
- 세션 종료 시 Raw Data와 다시 대조한 **Final Wiki**를 확정하고 저장한다.
- Final Wiki는 이후 토론의 AI 컨텍스트로 자동 승계하지 않는다.
- Final Wiki와 공식 토론 기록은 서로 관련되지만 같은 산출물이 아니다. Final Wiki는 세션 사고 구조의 내부 확정본이고, 공식 토론 기록은 참가자에게 보여주기 위해 별도로 검증·가공한 결과다.
- 공식 종료 후 Final Wiki와 공식 토론 기록이 AI에 의해 임의로 계속 변하지 않게 한다.

## 15.2 Living Wiki에서 유지해야 할 정보

1. **Current Topic**
   - 현재 실제 논점
   - 현재 질문
   - 논점 변화

2. **Perspective Map**
   - 주요 관점
   - 각 관점의 근거
   - 충돌/보완 관계
   - 관련 원본 reference

3. **Book Grounding**
   - 언급된 책 문장/장면/인물/주장
   - 어떤 관점과 연결되는지

4. **Issue & Question Map**
   - 핵심 쟁점
   - 아직 충분히 탐색되지 않은 열린 질문

5. **Coverage**
   - 아직 다루지 않음
   - 일부 탐색
   - 충분히 탐색

6. **Participant State**
   - 세션 안에서 공개적으로 표현된 참가자의 현재 관점과 주요 근거
   - 현재 참석, 최근 발언, 침묵 등 토론 운영에 필요한 참여 상태
   - 참가자의 내적 상태나 생각 변화를 근거 없이 단정하지 않음
   - 비공개 입력에서 파생된 상태는 PUBLIC 원본 기반 Wiki와 분리하고 별도 접근 제약을 유지

7. **Discussion Metrics & Key Changes**
   - 7개 Discussion Metrics
   - 현재 상황 요약
   - 토론 중 공개 발언으로 확인되는 중요한 관점 변화와 새 연결

## 15.3 근거와 표현 규칙

- 가능한 Wiki 항목에는 메시지, PUBLIC 사전 입력, AI 개입 또는 Book Context 항목을 가리키는 원본 reference를 붙인다.
- 관점, 관계, 책 근거, 생각의 변화처럼 해석이 개입되는 항목일수록 reference가 중요하다.
- reference는 원본을 복제한 요약문이 아니라 원본 위치를 다시 확인할 수 있는 식별자여야 한다.
- 원본에서 확인되지 않는 내용을 Wiki가 새로운 사실로 만들어서는 안 된다.
- 여러 관점이 충돌하면 하나로 합쳐 거짓 합의를 만들지 않고 충돌 관계와 각각의 근거를 보존한다.
- 실제 발언의 변화는 기록할 수 있지만 참가자의 내적 신념이 바뀌었다고 단정하지 않는다.
- `AI_PRIVATE` 원문이나 작성자를 추론할 수 있는 reference는 PUBLIC-lane Wiki에 포함하지 않는다.

## 15.4 업데이트 원칙

- 메시지 한 개마다 전체 Wiki를 무조건 다시 쓰지 않는다.
- 의미 있는 대화 묶음이 형성되어 토론 상태 평가가 실행될 때 Wiki를 점진적으로 갱신하고 같은 분석 주기에서 Discussion Metrics도 평가한다.
- 새 대화가 없는 운영 이벤트에서는 불필요하게 Wiki를 재작성하지 않아도 된다.
- 각 업데이트가 어느 메시지까지 반영했는지 기준점을 유지해야 한다.
- 주제 전환 시 해당 주제의 Wiki와 관련 Raw Data를 대조해 **Topic Checkpoint**를 만들고 필요한 부분을 보정한다.
- 세션 종료 시 필요한 Raw Data를 다시 조회해 전체 정합성을 검증하고 **Final Wiki**를 확정한다.
- 동시에 실행된 오래된 분석이 더 최신 Wiki를 덮어써서는 안 된다.
- Wiki 업데이트나 검증이 실패하면 마지막으로 확정된 Wiki를 유지하며 인간 대화와 세션 진행을 막지 않는다.
- Wiki의 해석은 원본 사실보다 우선하지 않는다.
- 복수 해석과 불일치를 거짓 합의로 평탄화하지 않는다.

---

# 16. Raw Data 활용 원칙

AI는 Living Wiki만으로 판단하기 어려울 때 필요한 원본을 다시 확인할 수 있다.

대표 상황:
- 판단 확신이 낮거나 모호함
- 특정 참가자의 이전 입장이나 근거 확인
- 비슷한 논의가 이미 있었는지 확인
- 이미 다룬 질문인지 확인
- 실제 관점 변화 여부 검증
- 사전 입력과 현재 대화 연결
- Wiki 내부 정보 충돌
- 주제 전환/세션 종료 시 정합성 확인

조회 결과는 검색된 요약문 하나만 근거로 사용하지 않고, 연결된 원본 메시지와 판단에 필요한 주변 문맥까지 다시 읽을 수 있어야 한다.

대상을 정확히 아는 경우에는 메시지·참가자·시각·의제·사전 입력 식별자를 사용해 직접 조회한다. 위치를 모르는 과거 논의를 찾을 때는 세션 안의 범위를 좁혀 관련 대화 묶음을 찾고, 그 결과가 가리키는 원본으로 내려간다.

원본 저장 단위는 개별 메시지로 유지한다. 검색을 위해 여러 메시지를 대화 묶음으로 파생하더라도 원본을 대체하거나 수정해서는 안 되며, PUBLIC과 AI_PRIVATE를 같은 검색 단위에 섞지 않는다.

절대 원칙:

> Raw Data는 사실의 source of truth이며 AI가 임의로 수정하지 않는다.

정확한 검색/저장/색인 구현 방식은 기술 기획에서 결정한다.

---

# 17. 비공개 정보 제품 규칙

- 사전 입력의 공개 범위는 `PUBLIC / AI_PRIVATE`로 구분한다.
- AI_PRIVATE는 내부 판단을 위해 사용할 수 있다.
- AI Host가 참가자에게 발언을 만들 때 비공개 원문을 노출해서는 안 된다.
- 참가자의 비공개 표현, 출처, 신원을 직접 또는 추론 가능하게 드러내서는 안 된다.
- 필요한 경우 비식별·추상화된 신호만 Host 행동에 사용할 수 있다.
- 이 규칙은 다른 Discussion Metric, weight, threshold보다 우선한다.

## 17.1 데이터 보관과 계정 탈퇴

- 사용자는 프로필 설정에서 현재 비밀번호와 삭제 영향을 확인한 뒤 계정을 탈퇴할 수 있다.
- 진행 중인 토론에 실제 참여 중이면 공식 종료 전까지 탈퇴할 수 없다.
- 예정된 방의 방장은 먼저 방장 권한을 이전하거나 방을 취소해야 한다.
- 일반 참가자의 시작 전 참가 등록은 탈퇴 시 자동 취소한다.
- `ADMIN` 계정은 역할을 수동 해제한 뒤 탈퇴할 수 있다.
- 탈퇴가 완료되면 로그인할 수 없고 복구할 수 없으며 같은 이메일로 새로 가입할 수 있다.
- 탈퇴 시 이메일, 비밀번호 정보, 현재 프로필 이름, 계정 설정과 모든 AI_PRIVATE 사전 입력을 삭제한다.
- 공동 원본 채팅, PUBLIC 사전 입력, 마지막 한 줄의 내용은 유지하되 작성자 이름을 `탈퇴한 사용자`로 바꾼다.
- 공동 기록 연결을 위해 내부에는 계정과 연결되지 않는 비식별 식별자만 남긴다.
- 이미 확정된 익명 토론 기록의 쟁점·관점·변화·남은 질문은 유지한다.
- 탈퇴에 따른 작성자 익명화는 공식 기록 불변 원칙의 개인정보 예외다.
- 사용자가 특정 메시지 하나를 직접 삭제하는 기능은 제공하지 않는다.
- 개인정보나 법적 문제가 있는 개별 콘텐츠 삭제는 제품 밖의 운영 요청으로 처리하고, 삭제된 위치에는 `운영 정책에 따라 삭제된 메시지입니다` 표식을 남긴다.
- 활성 계정과 프로필은 탈퇴할 때까지 보관한다.
- 원본 채팅, PUBLIC 사전 입력, Final Wiki와 공식 토론 기록은 별도 자동 만료 없이 보관한다.
- AI_PRIVATE는 작성자가 탈퇴하거나 운영 정책에 따라 삭제할 때까지 보관한다.
- AI 평가·Policy·Host 진단 로그는 세션 종료 후 90일 동안 보관하고 이후 삭제하거나 개인과 연결되지 않는 집계 데이터만 남긴다.
- Book Context 조사 자료, 출처, Published·Retired 버전과 운영자 감사 로그는 자동 만료 없이 보관한다.
- 백업에서의 최종 삭제 시점과 운영 삭제 절차는 기술·보안 설계에서 정한다.
- 전체 데이터 내보내기는 MVP에서 제공하지 않는다.
- 공개 서비스 전에는 적용 지역의 개인정보·보존 의무에 맞춰 별도 검토한다.

## 17.2 장애 시 복구 제품 규칙

- 연결 상태는 `연결됨 / 재연결 중 / 오프라인`으로 구분한다.
- 연결이 끊기면 기존 대화는 읽을 수 있지만 새 메시지와 운영 버튼은 동기화가 끝날 때까지 비활성화한다.
- 작성 중이던 입력 내용은 브라우저에 유지한다.
- 자동 재연결 후 누락 메시지, 현재 단계, 남은 시간, 역할, 참가자 상태를 동기화한 다음 입력을 다시 허용한다.
- 화면의 오래된 상태와 서버 상태가 다르면 서버 상태를 우선한다.
- 개인 연결 단절이나 서버 장애 중에도 세션 타이머는 계속 흐르며, 복구 후 방장은 반복 연장 기능으로 시간을 보충할 수 있다.
- 서버가 확정하지 않은 메시지는 확정 메시지처럼 표시하지 않는다.
- 전송 실패 메시지는 입력 내용을 유지하고 `다시 보내기`를 제공한다.
- 세션 시작, 방장 이전, 참가자 내보내기, 연장, 토론 종료는 서버 성공 확인 후에만 화면에 반영한다.
- 운영 명령 실패 시 기존 상태를 유지하고 방장에게 재시도를 안내한다.
- 메시지와 운영 명령의 중복 요청은 한 번만 반영한다.
- 자동 AI 평가·개입 실패는 인간 대화를 막지 않는다.
- Opening은 기본 질문, Synthesis는 Closing 전환, 연장 AI 의견은 방장 직접 결정으로 대체한다.
- AI 판단 중 새 대화로 개입 필요가 사라지면 오래된 AI 개입을 게시하지 않는다.
- AI Host 메시지도 서버에서 한 번만 확정한다.
- 세션 종료와 결과 생성을 분리하고 생성 중에도 종료된 채팅을 열람할 수 있게 한다.
- 결과 생성은 자동 재시도하며 `생성 중 / 재시도 중` 상태를 표시한다.
- 자동 재시도가 모두 실패하면 `토론 기록을 만들지 못했습니다`를 표시하고 방장에게만 `다시 생성하기`를 제공한다.
- 최초 공식 결과가 확정되기 전만 재시도할 수 있고 성공한 결과는 다시 생성하지 않는다.
- Builder 조사·재생성 실패 시 기존 Draft와 운영자 수정 내용을 유지하고 실패 단계와 재시도 가능 여부를 표시한다.
- 실패한 Pack은 자동으로 Review나 Published 상태가 되지 않는다.
- 원본 서버 오류, 모델명, stack trace, 내부 점수는 사용자에게 노출하지 않는다.
- 장애·재시도는 사용자 데이터 내용이 아니라 작업 상태 중심으로 감사·운영 로그에 기록한다.
- 복구할 수 없는 데이터 손상과 전체 서비스 재해복구 절차는 기술·인프라 계획에서 정한다.

---

# 18. 책 지식 — Book Context 제품 요구사항

MVP에서는 전체 책 본문 RAG를 필수 조건으로 두지 않는다.

핵심 가설은:

> AI가 좋은 토론 진행자로 개입하면 실제 독서토론의 질이 높아지는가?

책 전체 본문 검색은 이 가설의 필수 기능이 아니라 Book Grounding의 성능을 높이는 보조 수단으로 본다.

## 18.1 MVP

책이 선정되면 **Book Context Pack**을 준비한다.

Book Context Pack은 책을 대신 읽게 만드는 요약본이 아니라:

> AI 호스트가 참가자의 발언을 책 전체 구조, 장면, 쟁점과 연결하기 위한 재사용 가능한 호스트용 지식 지도.

참가자의 사전 입력과 세션 대화는 Book Context Pack에 섞지 않는다.

Evaluator와 AI Host는 특정 DB 테이블이나 요약 필드에 직접 의존하지 않고 **Book Context Provider**를 통해 책 정보를 받는다. MVP Provider는 운영자가 검수·Publish한 구조화 Pack을 반환한다. 향후 Full-text RAG가 필요해져도 Provider 내부 구현만 교체·확장하고 Evaluator, Policy Engine, AI Host의 제품 역할은 유지한다.

## 18.2 Pack 기본 구성

1. 기본 메타정보
   - 제목, 저자, 출판사, 출간연도, 장르, 판본/번역자, ISBN 등

2. 전체 구조와 흐름
   - 전체 개요
   - 장/부별 핵심 흐름

3. 핵심 주제와 문제의식
   - 주요 주제
   - 반복되는 긴장과 가치 충돌

4. 주요 인물 / 개념 / 사건
   - 소설의 인물·관계·사건
   - 비문학의 핵심 개념·사례·주장

5. 주요 장면 / 주장 지도
   - 토론 가치가 높은 장면 또는 주장
   - 위치
   - 관련 주제와 쟁점

6. 토론 가능 쟁점
   - 하나의 정답보다 서로 다른 해석이 가능한 충돌 지점
   - 관련 근거

7. 해석상 주의사항
   - 논쟁적 해석
   - 저자 의도와 비평적 해석의 구분
   - 복수 해석 가능성

## 18.3 지식 항목의 성격 구분

### FACT
확인 가능한 사실.

### AUTHOR_STATEMENT
저자가 직접 밝힌 의도/배경.

### INTERPRETATION
비평가, 연구자, 독자가 제시한 해석.

### DISCUSSION_SIGNAL
독자들이 실제로 궁금해하거나 의견이 갈리는 지점.

사실과 해석을 한 문장으로 합쳐 확정하지 않는다.

## 18.4 항목별 근거와 불확실성

각 지식 항목은 가능한 범위에서 다음 정보를 함께 보존한다.

- 어느 Pack 섹션에 속하는지
- `FACT / AUTHOR_STATEMENT / INTERPRETATION / DISCUSSION_SIGNAL` 중 어떤 성격인지
- 확인에 사용한 출처와 출처 등급
- 출처 안에서 근거를 다시 찾을 수 있는 위치 또는 설명
- 단일 출처인지, 독립적인 복수 출처가 일치하는지
- 정보가 부족한지
- 출처 사이에 충돌이 있는지

공식·고신뢰 출처라고 해서 모든 해석이 정답이 되는 것은 아니며, 낮은 등급의 출처라고 해서 토론 신호로서의 가치까지 사라지는 것도 아니다. 근거 수준은 출처 등급 하나를 숫자 점수처럼 사용하는 대신 근거의 성격을 설명하는 방식으로 표시한다.

웹에서 충분히 확인할 수 없는 장별 내용이나 장면은 억지로 채우지 않고 정보 부족 상태로 남긴다. 출처가 충돌하면 하나를 임의로 선택해 사실로 만들지 않고 충돌 상태와 각각의 근거를 보존한다.

---

# 19. Book Context Builder 제품 요구사항

운영자가 **제목, 저자 또는 ISBN으로 외부 도서 카탈로그를 검색하고 정확한 판본을 선택**하면 필요한 공개 자료를 자동 탐색해 Book Context Pack 초안을 생성하는 운영자용 준비 기능을 둔다. MVP 검색 Provider는 Kakao 책 검색을 사용하며 검색 결과가 없을 때 임의 수동 등록은 제공하지 않는다.

기대 흐름:

**외부 도서 검색 → 정확한 판본 선택 → 공개 자료 조사 → 사실/해석/토론신호 분류 및 교차검증 → Pack 초안 → 전체 내용 수정·삭제 → 전체 검수 완료 → Publish**

외부 도서 검색 결과는 책·판본 식별과 기본 서지정보 입력에만 사용한다. 외부 API의 소개 문구를 검수 완료된 Pack 내용으로 간주하지 않으며, 동일 ISBN 또는 동일 Provider 도서 식별자의 기존 Pack이 있으면 중복 생성하지 않는다.

운영자 계정과 권한:

- 모든 계정은 기본적으로 일반 회원이며 지정된 계정에만 `ADMIN` 역할을 부여한다.
- 운영자 지정·해제는 제품 UI가 아니라 배포 환경이나 관리 명령으로 수동 처리한다.
- 운영자 전용 회원가입, 초대, 역할 관리 UI는 MVP에서 제공하지 않는다.
- 운영자는 일반 로그인 후 운영자 전용 Builder 화면에 접근한다.
- 운영자 화면과 API는 서버에서 `ADMIN` 권한을 검증해야 한다.
- 운영자는 Pack 초안 생성, 조사 상태와 실패 확인·재시도, 항목·출처 검수, 항목 수정·삭제·재생성, Pack 상태 변경과 Publish, Publish 취소와 버전 관리를 수행할 수 있다.
- MVP에서는 편집자, 검수자, 게시자 역할을 분리하지 않는다.
- 운영자 계정도 일반 회원처럼 토론방을 생성·참가할 수 있으며 해당 토론에서는 일반 회원과 동일한 권한만 가진다.
- 운영자 여부는 다른 참가자에게 표시하지 않는다.
- 운영자는 운영자 권한만으로 참가하지 않은 토론방의 채팅, 사전 입력, 결과에 접근할 수 없다.
- 다른 사용자의 AI_PRIVATE 입력은 운영자에게도 기본적으로 공개하지 않는다.
- 운영자는 회원 비밀번호를 열람하거나 사용자를 사칭할 수 없다.
- 회원 관리, 신고 처리, 메시지 삭제, 세션 강제 개입은 운영자 도구의 MVP 범위에서 제외한다.
- Pack 생성·수정·삭제·Publish 등 운영자 동작은 실행 계정, 시각, 대상, 변경 내용을 감사 로그로 남긴다.

## 19.1 Builder 원칙

- 빈 필드를 그럴듯하게 채우는 것이 목표가 아니다.
- 확인 가능한 정보를 구조화한다.
- 정보 부족은 정보 부족으로 표시한다.
- 출처 충돌은 하나로 임의 결정하지 않는다.
- 자동 생성 결과를 운영자 승인 없이 실제 토론에 사용하지 않는다.

## 19.2 출처 활용 원칙

### Tier A
출판사 공식, 저자 공식/인터뷰, 도서관·공공기관, 공식 서지정보.

용도:
- 메타정보
- 저자 발언
- 공식 사실

### Tier B
학술논문/학술서, 전문 비평, 신뢰도 높은 문학·문화 전문 매체.

용도:
- 작품 분석
- 핵심 주제
- 해석
- 논쟁적 쟁점

### Tier C
주요 언론 서평, 대형 서점 소개/편집 리뷰.

용도:
- 줄거리/구조 보완
- 대중적 평가
- 쟁점 탐색

### Tier D
개인 블로그, 일반 독자 리뷰.

용도:
- 독자 관점과 토론거리 탐색
- 사실의 단독 근거로 사용하지 않음

### Tier E
커뮤니티, SNS, 익명 게시물.

용도:
- Discussion Signal
- 사실 확인 근거와 구분

## 19.3 정보 유형별 원칙

- 기본 사실/서지: 공식·공공 출처 우선
- 줄거리/구조: 복수 신뢰 출처로 교차 확인
- 저자의 문제의식/의도: 저자의 직접 발언과 타인의 해석을 분리
- 핵심 주제/작품 해석: 복수 해석을 보존
- 토론 쟁점: 전문 비평 + 서평 + 독자 반응 + 커뮤니티 논쟁까지 폭넓게 탐색 가능

요약:

> 공식 출처로 사실을 확인하고, 전문 출처로 작품을 이해하며, 다양한 독자 출처로 실제 토론거리를 발견한다.

## 19.4 전체 검수와 Publish Gate

자동 생성 Pack은 운영자가 명시적으로 Publish하기 전 실제 토론에서 사용할 수 없다.

운영자는 7개 섹션을 위에서 아래로 이어서 읽고 필요한 내용을 수정하거나 삭제한 뒤 Pack 단위로 `전체 검수 완료`를 선언한다. 항목이나 섹션마다 검수됨/미검수 상태를 지정하도록 요구하지 않는다.

운영자가 `항목 추가`만 누르고 제목과 내용을 모두 입력하지 않은 새 항목은 Draft 데이터에 저장하지 않는다. 제목 또는 내용 중 하나를 작성하기 시작한 항목은 두 값을 모두 필수로 입력해야 저장되며, 미완성 상태에서는 자동 저장과 전체 검수 완료를 보류한다.

운영자가 확인해야 할 요소:
- 책 식별 일치
- 핵심 FACT 출처 충돌
- Pack 주요 섹션 Coverage
- 출처 품질
- 정보 부족/불확실성
- 충돌이 숨겨지지 않았는지

자동 검사 결과는 운영자 화면에서 `수정 필요`와 `확인 필요` 두 단계로 구분하고, `근거 부족`, `검수 필요`, `출처 확인 불가`처럼 필요한 행동을 알 수 있는 문구로 표시한다. Tier A~E와 내부 validation code는 품질 판정에 유지하되 기본 운영자 화면에는 노출하지 않는다.

- `수정 필요`가 남아 있으면 전체 검수 완료와 Publish를 차단한다.
- `확인 필요`는 표시된 내용을 Pack 단위로 한 번 확인한 뒤 검수 완료할 수 있다.
- 전체 검수 완료와 Publish는 분리한다. 검수 완료는 내용 확인 선언이고 Publish는 신규 토론방 노출 결정이다.
- 검수 완료 상태에서 다시 수정하려면 Draft로 돌아가며 이전 revision의 검수 완료 기록은 재사용하지 않는다.

## 19.5 Pack 버전과 Publish 취소

- Pack 상태는 내부적으로 `Draft → Review → Published → Retired`로 관리하고 운영자 화면에는 `작성 중 → 검수 완료 → 게시됨 → 게시 중단`으로 표시한다. Builder가 실행 중인 Draft는 `생성 중`으로 표시한다.
- Draft만 직접 수정할 수 있으며 Review에서 수정이 필요하면 Draft로 되돌린다.
- Published 버전은 직접 수정하거나 덮어쓸 수 없다.
- Published Pack을 수정하려면 기존 내용을 복제한 새 Draft 버전을 만든다.
- 버전은 책별 `v1, v2, v3...`처럼 증가한다.
- 한 책에는 신규 토론방에서 사용할 활성 Published 버전을 하나만 둔다.
- 새 버전을 Publish하면 기존 활성 버전은 Retired로 전환하고 신규 방에는 새 버전을 노출한다.
- 토론방은 방 생성 시점에 선택한 정확한 Pack 버전을 고정해서 참조한다.
- 새 버전 Publish나 기존 버전 Retire는 이미 생성된 방, 진행 중인 세션, 종료 결과의 Pack 버전을 자동 변경하지 않는다.
- 일반적인 Publish 취소는 버전을 Retired로 전환하고 신규 방 책 목록에서 즉시 숨긴다.
- Retired 버전을 참조하는 기존 방은 해당 고정 버전으로 시작·진행·열람할 수 있다.
- 심각한 오류로 기존 방까지 중단하는 긴급 폐기 기능은 MVP에 포함하지 않고 운영 사고 대응 대상으로 남긴다.
- 활성 Published 버전이 없으면 해당 책은 신규 방 생성 목록에 표시하지 않는다.
- Draft 편집 내용은 자동 저장하고 현재 저장 상태를 표시한다.
- 전체·항목별 재생성은 현재 Draft에 반영하기 전에 변경 영향을 확인받는다.
- Publish와 Publish 취소에는 확인 절차를 제공한다.
- Publish 취소 시 운영 사유를 필수로 입력한다.
- 버전을 삭제하지 않고 Retired 상태와 전체 버전·상태 변경 이력을 감사 로그에 보존한다.

구체 UI는 기술/UX 기획에서 결정한다.

---

# 20. Full-text RAG에 대한 제품 입장

MVP에서는 구현하지 않는다.

원칙:

> **RAG-ready, not RAG-first.**

향후 실제 테스트에서 책의 세부 내용 부족이 AI 호스팅 품질의 병목으로 확인될 때 도입을 검토한다.

전체 본문 저장, 색인, 외부 모델 전달 등을 도입할 경우 도서관의 이용 권한, 저작권, 계약 조건을 별도로 검토한다.

---

# 21. MVP 사용자 검증과 설문 제외

내부 Discussion Metrics 점수를 높이는 것 자체가 목표가 아니다.

> **사용자에게 의미 있는 사고 확장이 일어나는 것이 제품이 지향하는 Outcome이며, Discussion Metrics는 토론 흐름과 AI 개입을 개선하기 위한 Diagnostic이다.**

## 21.1 인앱 설문 제외

- MVP 제품에는 세션 종료 설문 기능을 포함하지 않는다.
- 사고 확장, 만족도, AI 개입 도움 정도, 과잉 개입 체감, 재참여 의향 등을 앱 안에서 점수로 수집하지 않는다.
- 파일럿에서 사용자 Outcome 확인이 필요하면 제품 기능과 분리된 인터뷰나 운영 리서치로 진행할 수 있다.

## 21.2 내부 Diagnostic

최소한 다음 관계는 내부적으로 분석할 수 있어야 한다.

- 7개 Discussion Metrics
- Policy Action
- 실제 AI 개입
- 개입 전후의 대화 변화와 참가자 반응

예:
- `EXPAND` 후 Expansion이 실제 증가했는가
- `DEEPEN` 후 Depth가 증가했는가
- `RECONNECT`가 흐름을 살렸는가

비슷한 수준의 좋은 대화 흐름을 더 적은 AI 개입으로 유지하는 것은 긍정적인 운영 신호다. 단, AI 발언 수 최소화 자체를 독립적인 목표로 삼지는 않는다.

---

# 22. 아직 확정값이 아닌 것

다음은 구현 전에 억지로 고정하지 않고 기술 기획 또는 사용자 테스트에서 결정/보정한다.

- Discussion Metrics의 실제 threshold
- LOW/HIGH 지속 횟수
- Activity의 정확한 시간 기준
- weight의 실제 계산 방식
- 활성 참가자의 구체 정의
- 동시 메시지와 AI 개입 간 race condition 처리
- AI 원본 검색의 호출 비용/지연 제한
- confidence 사용 방식의 구체화
- 30분 텍스트 세션의 적정성
- Book Context Builder 상세 검수 UI
- 전체 본문 RAG 도입 여부
- 세부 검색/저장/색인 방식

이 항목들은 **제품 원칙을 변경하지 않는 범위**에서 기술 설계와 실험으로 결정한다.

---

# 23. 제품 기획 완료 기준

현재 제품 기획에서 확정된 것:

- 제품 목표
- 좋은 토론의 정의
- AI 최소 개입 철학
- 7개 Discussion Metrics
- Policy 행동 우선순위
- 계정·프로필·검색·패스워드 기반 참가 정책
- 30분 토론 타이머와 최대 5분 Closing Flow
- 횟수 제한 없는 15분 단위 반복 연장 정책
- Synthesis
- Closing
- 「오늘의 토론 기록」
- 종료 후 기록 열람·공유·익명화 정책
- 방장·참가자·운영자 권한
- 사전 입력과 AI_PRIVATE 정책
- 실시간 메시지·presence·재접속·장애 복구 정책
- 탈퇴·데이터 보관 정책
- Living Wiki의 7개 섹션, 원본 reference, Topic Checkpoint와 Final Wiki 확정 원칙
- Raw Data source-of-truth와 원본 재확인 원칙
- Book Context Provider, Pack 7개 섹션과 항목별 성격·출처·불확실성
- Book Context Builder의 버전·검수·Publish 요구사항
- Full-text RAG의 MVP 제외 원칙
- MVP 사고 확장 목표와 인앱 설문 제외 원칙

이제부터의 기술 아키텍처, 데이터 모델, API, 실시간 처리, 코드 구조, LLM orchestration, 저장/검색 구현과 같은 사항은 **Codex에서 별도 기술 문서로 설계한다.**

---

# 24. 한 문장 요약

> 완독한 사람들이 실시간 텍스트로 책을 토론할 때, AI는 좋은 인간 대화를 우선 보존하면서 필요한 순간에만 최소 개입한다. 세션은 Opening → Core Discussion → 종료 임박 시 연장 판단 → Synthesis → Closing으로 운영하며, 마지막에는 「오늘의 토론 기록」으로 실제 쟁점·관점·생각의 변화와 확장·남은 질문을 익명 중심으로 남긴다. 사용자의 사고 확장은 제품이 지향하는 Outcome이며, MVP에서는 인앱 설문 없이 내부 Discussion Metrics와 개입 전후의 대화 변화를 개선을 위한 진단 자료로 사용한다.
